#include <stdio.h>
int main()
{
    int x=10;
    int y=15;
    int z=0;
    /*find the largest between x,y and z*/
    zs=(x>y) ? x : y;
    printf ("\n x=%d,y=%d,z=%d",x,y);
    printf ("\n largest is %d",z);
    return 0;
}