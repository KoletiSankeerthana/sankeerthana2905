#include <stdio.h>

int main() {
    int number; // Declare an integer variable to store the input

    // Prompt the user for input
    printf("Enter an integer: ");

    // Read the integer from the user
    scanf("%d", &number);

    // Print the integer
    printf("You entered: %d\n", number);

    return 0;
}
